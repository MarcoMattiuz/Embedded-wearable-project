# Bitmaps.h/.c
the files contain the declaration and definition of bitmaps (uint8_t arrays) that represent images

# font5x7.h/.c
the files contain the declaration and definition of bitmaps that represent the ascii alphabet. Each charachter is 5x7 pixel

